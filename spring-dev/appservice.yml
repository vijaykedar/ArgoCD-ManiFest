apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: wezvaspringbootapp
  name: backend-svc
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
    name: http
    nodePort: 30000
  selector:
    app.kubernetes.io/name: wezvaspringbootapp
  type: NodePort

