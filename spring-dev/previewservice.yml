apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: wezvaspringbootapp
  name: backendnew-svc
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
    name: http
    nodePort: 31000
  selector:
    app.kubernetes.io/name: wezvaspringbootapp
  type: NodePort
